@model Locatic.Models.CarModel

@{
    ViewData["Title"] = "Supprimer un modèle";
    var deleteError = ViewData["DeleteError"] as string;
}

<h1>Supprimer un modèle</h1>

@if (!string.IsNullOrWhiteSpace(deleteError))
{
    <div class="alert alert-danger">@deleteError</div>
}

<p class="text-danger">Êtes-vous sûr de vouloir supprimer ce modèle ?</p>

<dl class="row">
    <dt class="col-sm-3">Modèle</dt>
    <dd class="col-sm-9">@Model.Name</dd>

    <dt class="col-sm-3">Marque</dt>
    <dd class="col-sm-9">@Model.Brand.Name</dd>
</dl>

<form asp-action="Delete">
    <input type="hidden" asp-for="Id" />
    <button type="submit" class="btn btn-danger">Supprimer</button>
    <a asp-action="Index" class="btn btn-secondary">Annuler</a>
</form>