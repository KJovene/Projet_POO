@model IEnumerable<Locatic.Models.CarBrand>

@{
    ViewData["Title"] = "Marques";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h1>Marques</h1>
    <a asp-action="Create" class="btn btn-primary">+ Ajouter</a>
</div>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Marque</th>
            <th>Pays</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var brand in Model)
        {
            <tr>
                <td>@brand.Name</td>
                <td>@brand.Country</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@brand.Id" class="btn btn-sm">Modifier</a>
                    <a asp-action="Delete" asp-route-id="@brand.Id" class="btn btn-sm btn-danger">Supprimer</a>
                </td>
            </tr>
        }
    </tbody>
</table>