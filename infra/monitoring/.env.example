# Copier en .env (ignoré par Git) puis renseigner des valeurs locales.
# La stack fonctionne sans .env grâce aux valeurs par défaut du docker-compose.
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=change-me-locally
